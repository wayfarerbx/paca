FROM python:3.12-slim

# Install uv and watchfiles for hot reload
RUN pip install uv watchfiles

WORKDIR /app

COPY pyproject.toml .
RUN uv pip install --system -e .

ENV PYTHONPATH=/app
CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8080", "--reload"]
