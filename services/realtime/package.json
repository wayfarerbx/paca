{
  "name": "paca-realtime",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "check": "tsc --noEmit",
    "lint": "biome check ."
  },
  "dependencies": {
    "ioredis": "^5.4.2",
    "pino": "^9.7.0",
    "pino-pretty": "^13.1.3",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "@types/bun": "^1.2.0",
    "typescript": "^5.8.3"
  }
}
